Since the size of the help screens is fixed, screenshots should not
exceed 569 pixels in width (which btw is what most currently have).

Please resize if it is a full screen. Also, maximum recompression is
recommended:

optipng -o7 (loosles)
pngnq       (lossy, but it's the one currently used).
